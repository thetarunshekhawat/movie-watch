# Browser-automation tool output
.gstack/

# Agent worktrees — these are full checkouts of this repo, not source
.claude/

# Movie and subtitle files — never commit these
*.mp4
*.mkv
*.avi
*.mov
*.webm
*.srt
*.vtt

.DS_Store

# Film stills. Studio-copyrighted — populated locally via tools/, never
# committed, and never redistributed from the public Pages deployment.
img/frames/
img/frames-urls.json
